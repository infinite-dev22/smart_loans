import 'package:flutter/cupertino.dart';

class LoanPaymentsInitialWidget extends StatelessWidget {
  const LoanPaymentsInitialWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No loan payments currently"),
    );
  }
}
