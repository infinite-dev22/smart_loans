import 'package:flutter/cupertino.dart';

class LoanSchedulesInitialWidget extends StatelessWidget {
  const LoanSchedulesInitialWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No loan schedules currently"),
    );
  }
}
