import 'package:flutter/cupertino.dart';

class LoansInitialWidget extends StatelessWidget {
  const LoansInitialWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No loan currently"),
    );
  }
}
