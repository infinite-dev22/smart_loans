import 'package:flutter/cupertino.dart';

class LoanDetailsInitialWidget extends StatelessWidget {
  const LoanDetailsInitialWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No loan details currently"),
    );
  }
}
