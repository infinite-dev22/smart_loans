import 'package:flutter/cupertino.dart';

class EmployeesInitialWidget extends StatelessWidget {
  const EmployeesInitialWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Your employees appear here"),
    );
  }
}
