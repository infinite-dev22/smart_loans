import 'package:flutter/cupertino.dart';

class DocumentsInitialWidget extends StatelessWidget {
  const DocumentsInitialWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Your loan documents appear here"),
    );
  }
}
