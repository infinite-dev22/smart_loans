import 'package:flutter/cupertino.dart';

class ClientsInitialWidget extends StatelessWidget {
  const ClientsInitialWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Your clients appear here"),
    );
  }
}
